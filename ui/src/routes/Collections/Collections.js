import React from "react";

function Collections() {
    return (
        <React.Fragment>
            Collections - need server Id
        </React.Fragment>
    );
}

Collections.propTypes = {}

export default Collections;
